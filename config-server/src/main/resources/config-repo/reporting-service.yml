server:
  port: 8086

spring:
  application:
    name: reporting-service

  datasource:
    url: jdbc:postgresql://localhost:5432/electric_store
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

logging:
  level:
    root: INFO
    org.springframework: WARN
