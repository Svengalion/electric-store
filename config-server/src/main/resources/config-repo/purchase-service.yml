server:
  port: 8083

spring:
  application:
    name: purchase-service

  datasource:
    url: jdbc:postgresql://localhost:5432/electric_store
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: INFO
    org.springframework: WARN
